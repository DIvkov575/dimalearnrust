do primitive valeus get cloned?
^^ when do values get cloned / alternatives
if let

is typecasting into usize/isize intensive
what is inlining
     - const vs static

I -> U time even if positive

emacs
 - debugging - creating break points + viewing all var values
